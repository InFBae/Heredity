using UnityEngine;
using UnityEngine.XR.Interaction.Toolkit;

public class ShakeInteractor : XRBaseInteractor
{
    /*
    protected override void OnSelectEntered(SelectEnterEventArgs args)
    {
        base.OnSelectEntered(args);

        ShakeInteractable shakeInteractable = args.interactable as ShakeInteractable;
        if (shakeInteractable != null)
        {
            shakeInteractable.Shake(this); // 현재 Interactor를 전달하여 Shake 메서드를 호출.
        }
    }
    */
}